 
    import '../repositories/repositories.dart';

    class GetSettings_profileUseCase {
        final Settings_profileRepository repository;
      
        GetSettings_profileUseCase({required this.repository});
      
        // Future<User> execute(String userId) async {
        //   return userRepository.getUser(userId);
        // }
      }
      